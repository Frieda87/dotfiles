name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🍏 Doing some spell check"
      - run: pip install proselint
      - run: proselint ~/README.md 

